โอเค ทำตัวเลือก C เลย ✅ ด้านล่างคือ **ไฟล์ `PROJECT_MAP.md` แบบก๊อปวาง** (ตั้งใจทำให้เป็น “แผนที่โปรเจกต์” ที่คุณเปิดเมื่อไหร่ก็รู้ทันทีว่าโปรเจกต์ทำอะไรอยู่ โครงสร้างเป็นไง และจะเพิ่มฟีเจอร์ตรงไหน)

> วิธีใช้: สร้างไฟล์ `PROJECT_MAP.md` ที่ root repo แล้ววางทั้งหมดนี้ จากนั้นแก้เฉพาะส่วนที่มี `TODO:` ตามจริงของคุณ

```markdown
# PROJECT_MAP — bingx-agent

> เอกสารนี้คือ “แผนที่โปรเจกต์” สำหรับให้ทีม/AI เข้าใจระบบใน 3 นาที  
> เป้าหมาย: วันไหนมีไอเดียเพิ่มฟีเจอร์ → เปิดไฟล์นี้แล้วรู้ทันทีว่าแก้ตรงไหน ไม่ต้องไล่โค้ดใหม่

---

## 0) TL;DR (ระบบทำอะไร)

- ระบบนี้เป็น Node.js service + Dashboard
- โฟลว์หลัก:  
  **Snapshot (ดึงข้อมูลตลาด) → Market Decision Engine (STEP01) → Thai Visual Interpretation (STEP02) → Dashboard แสดงผล**
- มีไฟล์ JSON/Cache หลายตัวไว้เก็บผลและ state ล่าสุดเพื่อใช้งานต่อเนื่อง

---

## 1) Repo Structure (ภาพรวมโฟลเดอร์)

> TODO: วางผลลัพธ์ `tree -a -L 4` ที่นี่ เพื่อให้แม่น 100%

ตัวอย่างโครงสร้าง (ปรับให้ตรง repo จริง):
```

bingx-agent/
dashboard/                  # UI / Next.js dashboard (แสดงผลและกด snapshot)
routes/                     # API routes ฝั่ง Node (snapshot, news context, plan-status ฯลฯ)
server.cjs                  # entry point ของ server
package.json
package-lock.json

# runtime outputs / snapshots / caches (ตั้งใจเก็บไว้เพื่อให้ดูได้)

market_snapshot.json
news_context.json
derivatives_history_cache.json
volatility_baseline_cache.json
plan_status_state.json
plan_status_log.jsonl
latest_decision.json
latest_step2.txt

```

---

## 2) Key Concepts / Terminology

### STEP01 — AI Market Decision Engine
- อินพุตหลัก: `market_snapshot.json` (+ `news_context.json` ถ้ามี)
- เอาต์พุต: JSON “decision” ที่มี `market_mode`, `risk_warning`, `reason`, `levels`, `parameters_for_grid_or_trend`, `summary_for_bot`
- กฎสำคัญ:
  - ห้ามใช้เว็บดึงราคา/แท่งเทียนแทน JSON
  - Freshness enforcement: ใช้ข้อมูลล่าสุดในไฟล์เท่านั้น
  - เพิ่ม field ได้เฉพาะใต้ `levels` เช่น `levels.trend`, `levels.smc`

### STEP02 — JSON → Thai Pro Trader Visual Interpretation
- รับ JSON จาก STEP01
- แปลให้เป็นภาษาไทยแบบ “อ่านแล้วเห็นภาพบนกราฟทันที”
- ห้ามเดาตัวเลขใหม่ / ห้ามเสริมความเห็นนอก JSON

---

## 3) Data Flow (แผนภาพการไหลของข้อมูล)

```

[User / Scheduler / Button]
|
v
(A) run_full_snapshot
|
v
market_snapshot.json + caches
|
v
(B) STEP01 decision engine
|
v
latest_decision.json
|
v
(C) STEP02 Thai visual interpretation
|
v
latest_step2.txt (หรือ json/text output)
|
v
Dashboard แสดงผล + Plan Status + Logs

````

> TODO: ถ้าคุณมี endpoint/สคริปต์ชื่อจริงของแต่ละขั้น ให้เติมในหัวข้อ 4

---

## 4) Endpoints / Commands (จุดเข้าใช้งาน)

### 4.1 Snapshot Runner
- Endpoint: `GET /run_full_snapshot?symbol=BTC-USDT&klineLimit=200&depthLimit=50`
- Purpose: สร้าง snapshot ล่าสุด + อัปเดตไฟล์ cache ที่ระบบใช้ต่อ
- Output:
  - `market_snapshot.json`
  - (optional) `news_context.json`
  - caches ต่าง ๆ เช่น derivatives / volatility / state

> TODO: ใส่ path จริงถ้ามีไฟล์ route แยก เช่น `routes/runFullSnapshot.cjs`

### 4.2 Plan Status API (Dashboard)
- Endpoint: `GET /api/plan-status` (ฝั่ง Next.js API route)
- Purpose: ให้ dashboard อ่าน decision ล่าสุด, state, logs, และประมวลผล display

> TODO: ระบุไฟล์ Next route จริง เช่น `dashboard/app/api/plan-status/route.ts`

---

## 5) Important Files (ไฟล์สำคัญต้องรู้จัก)

### 5.1 Source Code
- `server.cjs` — จุด start server + mount routes
- `routes/` — endpoints ฝั่ง Node ทั้งหมด
- `dashboard/` — UI แสดงผล (Market card, Plan tracker, buttons, logs)

### 5.2 Runtime Data (ตั้งใจให้ commit เพื่อดูย้อนหลัง/ให้ AI เห็น)
- `market_snapshot.json` — ข้อมูลตลาดล่าสุด (OHLC, orderbook, derivatives, session, volatility)
- `news_context.json` — ข่าว + risk overlay
- `derivatives_history_cache.json` — history ของ OI/Funding เพื่ออ่าน “Price vs OI”
- `volatility_baseline_cache.json` — baseline volatility เพื่อเทียบ ATR/BBW ratio
- `plan_status_state.json` — state ล่าสุดของ plan tracker
- `plan_status_log.jsonl` — log แบบไทม์ไลน์
- `latest_decision.json` — decision ล่าสุดจาก STEP01
- `latest_step2.txt` — สรุปภาษาไทยล่าสุดจาก STEP02

> NOTE: ไฟล์พวกนี้ “เปลี่ยนตลอด” ทำให้ git diff เยอะ เป็นเรื่องปกติ

---

## 6) Current Status (ทำถึงไหนแล้ว)

> TODO: เติมให้ตรงกับสถานะปัจจุบัน

### ✅ Done
- [x] โครงสร้าง Decision Engine STEP01 v6 (freshness enforcement + trend zone builder)
- [x] STEP02 prompt: แปล JSON เป็นภาษาไทยแบบเห็นภาพ
- [x] Dashboard แสดง Market Status / Plan Tracker / Copy summary
- [x] Snapshot endpoint ทำงานและอัปเดต caches/state ได้
- [x] Repo push ขึ้น GitHub และใช้ branch `main`

### 🚧 In Progress / Next
- [ ] ทำ “Project Snapshot” ให้ dashboard auto refresh หลัง snapshot เสร็จ
- [ ] เพิ่มปุ่ม snapshot + cooldown + log history การกด (ถ้ายังไม่จบ)
- [ ] จัดระเบียบ LF/CRLF ด้วย `.gitattributes` (ลด diff noise)
- [ ] เพิ่มเอกสารการตั้งค่า `.env.example` และ README

---

## 7) Feature Extension Guide (เพิ่มฟีเจอร์ต้องแก้ตรงไหน)

### 7.1 เพิ่มข้อมูลใหม่ใน snapshot
ไปที่:
- `routes/` (snapshot route)
- อัปเดต schema ที่เขียนไฟล์ `market_snapshot.json`
- อัปเดต dashboard reader ถ้าต้องแสดงเพิ่ม

### 7.2 เพิ่ม logic วิเคราะห์ใน STEP01
ไปที่:
- ส่วน Decision Engine ที่สร้าง `latest_decision.json`
- ห้ามเพิ่ม top-level field ใหม่
- เพิ่มรายละเอียดได้เฉพาะใน `levels.*`

### 7.3 เพิ่มรูปแบบสรุปภาษาไทย (STEP02)
ไปที่:
- prompt ของ STEP02 (ไฟล์เอกสารหรือ config ที่เก็บ prompt)
- ต้องยึด JSON เป็นแหล่งเดียว ห้ามเดาตัวเลขใหม่

### 7.4 เพิ่ม UI Card ใหม่บน Dashboard
ไปที่:
- `dashboard/components/*`
- `dashboard/app/public/page.tsx` (หรือไฟล์หน้าหลักจริง)
- เชื่อมกับ `/api/plan-status`

---

## 8) Dev Commands (รันในเครื่อง)

> TODO: ใส่คำสั่งจริงจาก package.json

ตัวอย่าง:
```bash
npm install
npm run dev
# หรือ
node server.cjs
````

---

## 9) Security Rules (ห้ามพลาด)

* ห้าม commit `.env` หรือ keys ใด ๆ ขึ้น GitHub
* ใช้ `.env.example` แทน
* ถ้า key หลุดแล้ว: หมุนคีย์ทันที

---

## 10) Notes / Decisions (บันทึกการตัดสินใจสำคัญ)

* ตั้งใจ commit ไฟล์ runtime snapshots/caches เพื่อให้ AI/ทีมดู state ล่าสุดได้
* ยอมรับว่า diff จะเยอะจากการอัปเดตตามเวลา
* โครงสร้าง STEP01/STEP02 แยกชั้นเพื่อให้ปรับ prompt/logic ได้อิสระ

---

## 11) TODO: Fill-in Checklist (กรอกครั้งแรกให้ครบ)

* [ ] วาง `tree -a -L 4` ในหัวข้อ 1
* [ ] ใส่รายชื่อ endpoint/ไฟล์จริงในหัวข้อ 4
* [ ] เติมคำสั่งรันจริงจาก `package.json` ในหัวข้อ 8
* [ ] เติม “Done / Next” ให้ตรงงานจริงในหัวข้อ 6

```

---

### ต่อไปคุณทำอะไร?
1) สร้างไฟล์ `PROJECT_MAP.md` ใน root repo แล้ววางเนื้อหาข้างบน  
2) รัน `tree -a -L 4` แล้วเอามาแทนในหัวข้อ 1 (จะทำให้เอกสารนี้ “ล็อคโครงสร้างจริง”)

ถ้าคุณแปะ output ของ `tree -a -L 4` กับ `type package.json` มาให้ผม 2 อย่างนี้  
ผมจะช่วย “เติม PROJECT_MAP ให้ตรงของจริง” และจัดเป็นเวอร์ชันสุดท้ายที่พร้อมใช้งานทันทีครับ.
```
